nelmio_api_doc:
    documentation:
        info:
            title: WordPopularity
            description: Calculates word popularity!
            version: 0.1.2
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
        disable_default_routes: true
